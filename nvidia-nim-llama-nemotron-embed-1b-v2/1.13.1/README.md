# nvidia-nim-llama-nemotron-embed-1b-v2

Generated by SUSE AI Factory

## Metadata
- **Name**: nvidia-nim-llama-nemotron-embed-1b-v2
- **Version**: 1.13.1
- **Description**: Application chart for NIM to be deployed using helm
- **Support**: opensource
- **Tier**: certified
- **Status**: new
- **Category**: Inference

## Authors
- alessandro.festa1@suse.com (Owner)

## Stack Composition
This stack includes the following components:

- **Argo Workflows** (argo-workflows): Argo Workflows is an open source container-native workflow engine for orchestrating parallel jobs on Kubernetes. Argo Workflows is implemented as a Kubernetes CRD (Custom Resource Definition). (Version: )
- **** ():  (Version: )

## Installation
To install this stack on your Kubernetes cluster, ensure you have Helm installed and run:

```bash
helm upgrade --install nvidia-nim-llama-nemotron-embed-1b-v2 ./chart -n nvidia-nim-llama-nemotron-embed-1b-v2 --create-namespace
```

## Upgrade
To upgrade an existing installation with new values or a newer version of the chart:

```bash
helm upgrade nvidia-nim-llama-nemotron-embed-1b-v2 ./chart -n nvidia-nim-llama-nemotron-embed-1b-v2
```

## Teardown
To remove the stack and all its components from your cluster:

```bash
helm uninstall nvidia-nim-llama-nemotron-embed-1b-v2 -n nvidia-nim-llama-nemotron-embed-1b-v2
```
