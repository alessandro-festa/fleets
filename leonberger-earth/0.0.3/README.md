# leonberger-earth

Generated by SUSE AI Factory

## Metadata
- **Name**: leonberger-earth
- **Version**: 0.0.3
- **Description**: ss
- **Support**: opensource
- **Tier**: certified
- **Status**: new
- **Category**: Inference

## Authors
- alessandro.festa1@suse.com (Owner)

## Stack Composition
This stack includes the following components:

- **PostgreSQL** (postgresql): PostgreSQL is an advanced object-relational database management system that supports an extended subset of the SQL standard, including transactions, foreign keys, subqueries, triggers, user-defined types and functions. (Version: )
- **vLLM** (vllm): A high-throughput and memory-efficient inference and serving engine for LLMs. (Version: )
- **PyTorch** (pytorch): PyTorch is a machine learning library based on the Torch library, used for applications such as computer vision and natural language processing (Version: )

## Installation
To install this stack on your Kubernetes cluster, ensure you have Helm installed and run:

```bash
helm upgrade --install leonberger-earth ./chart -n leonberger-earth --create-namespace
```

## Upgrade
To upgrade an existing installation with new values or a newer version of the chart:

```bash
helm upgrade leonberger-earth ./chart -n leonberger-earth
```

## Teardown
To remove the stack and all its components from your cluster:

```bash
helm uninstall leonberger-earth -n leonberger-earth
```
